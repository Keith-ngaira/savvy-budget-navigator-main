import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.savvynav.budget',
  appName: 'Savvy Budget Navigator',
  webDir: 'dist'
};

export default config;
